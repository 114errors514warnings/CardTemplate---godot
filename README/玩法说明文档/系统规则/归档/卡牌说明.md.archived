> ⚠️ **本文档已迁移（2026-08-29）**。内容已迁到 [卡牌与配置/State 牌 vs Skill 施加 State.md](卡牌与配置/State牌与Skill施加State.md)。详见 [README.md 总览](README.md) 附录 A。下方为原文保留。

# 卡牌说明

## State牌与Skill施加State的区别

### State牌的生命周期

1. 打出State牌后，该牌移动到目标单位的状态牌区（不进入弃牌堆）。
2. 状态效果持续期间，该牌保留在状态牌区。
3. 状态持续时间结束时，将该牌移回其所属角色的弃牌堆。
4. State牌占用状态牌区槽位，可被效果（如ClearState、ClearAllStates）清除或移除。

### Skill牌施加State

1. 打出带有AddState效果的Skill牌后，Skill牌直接进入弃牌堆。
2. 仅施加的状态标记进入目标单位的状态栏（状态牌区不增加牌）。
3. Skill牌本身不进入状态牌区，因此无法被ClearState等效果以"牌"的形式移除（但施加的状态标记本身仍可被清除）。

### 示例对比

| 类型 | 示例卡牌 | 打出后卡牌去向 | 状态去向 |
|------|----------|---------------|----------|
| State牌 | 反攻号角 | 目标状态牌区 | 目标状态栏 |
| State牌 | 勇气铠甲 | 自身状态牌区 | 自身状态栏 |
| Skill牌+AddState | （暂无） | 弃牌堆 | 目标状态栏 |
