6.5
1、新增独立的角色卡牌战斗主场景 `Scenes/Battle/CardBattleScene.tscn`，场景内直接复用现有 `BattleSytem`，并按“左侧角色区 / 中央日志区 / 右侧怪物区 / 下方手牌与牌堆控制区”的结构完成基础布局。
2、新增 `Scripts/UI/CardBattleScene.cs` 作为场景主控脚本，完成以下首版交互：单位区渲染、血条与状态图标显示、状态 tooltip 跟随鼠标显示、手牌区角色切换、卡牌拖拽打出、目标卡箭头指示、抽牌堆/弃牌堆/消耗牌堆弹窗切换，以及结束回合按钮与现有战斗流程的联动。
3、将 `Scenes/Card/CardDisplayPrefab.tscn` 中的名称、类型、费用和描述文字默认改为黑色，以满足新主场景中卡牌展示的基础视觉要求。
4、在 `README/施工文档/施工文档.md` 中补记“先将需求润色为可执行规格，再按规格实施并记录”的执行规则，并将本次新战斗场景需求的润色版规格落档，作为后续继续施工该场景的直接依据。
5、将项目启动场景切换为 `Scenes/Battle/CardBattleScene.tscn`，使游戏启动后直接进入新的战斗主场景，而不是旧的 `Scenes/Debug/DebugBattle.tscn`。
6、新增 `Scenes/UI/BattleSetupWindow.tscn` 作为战前初始化窗口，在战斗开始前默认弹出；窗口内独立复用了旧主控台的角色、怪物、额外卡牌配置按钮，并保留 `局内信息` 的战前配置展示能力。
7、新增 `Scenes/UI/BattleDebugPanelWindow.tscn` 作为调试面板窗口，仅迁移旧 `DebugBattle` 中 `操作面板` 的按钮功能，通过 `CardBattleScene` 中的按钮呼出，并支持关闭。
8、新增 `Scripts/System/Modules/SceneConsoleRouter.cs`，将新主场景下原本写入场景 `Console` 组件的消息改为直接输出到 Godot Console；同时保留旧 `DebugBattle` 存在时继续写入旧可视 Console 的兼容路径。
9、调整 `BaseButtonCommand`、`BattleInfoUiBinder`、`BattleSytem` 与 `CardBattleScene`，让嵌套窗口中的参数框查找、战前信息面板绑定、外部窗口触发后的战场 UI 刷新都能在新主场景结构下正常工作。

6.9
1、修复战前设置中删除角色按钮的匹配逻辑：输入框中输入ID则匹配对应角色（有多个匹配项时删除最后一个），输入为空则默认删除最后一个；无匹配项时控制台弹提示，不删除。
2、修复战前设置中删除敌人按钮：输入框为空时默认删除最后一个敌人。
3、放大手牌尺寸（缩放比 0.36→0.55），拖动预览放大（0.46→0.65）；手牌超出视口时自动缩进卡牌间距（下限 -20px），支持卡牌重叠；右侧卡牌通过 ZIndex 覆盖左侧。
4、修复卡牌无法拖动问题：禁用 ScrollContainer 在拖动期间的水平滚动，改用 _Input 全局鼠标事件 + 位置命中检测（GetGlobalRect.HasPoint）替代 GuiInput，避免 ScrollContainer 吞噬鼠标事件。
5、新增护盾值显示：单位血条右侧显示蓝色 "▣ 护盾值" 标签，无护盾时隐藏。
6、修复战前设置窗口不可见问题：`BattleSetupWindow` 使用 container 布局模式被嵌套在 position 模式父节点下，导致实际尺寸为零；强制设置 FullRect 锚点与偏移修正。
7、点击"查看战前设置"按钮后输出 GD.Print 日志辅助排查；为关闭按钮增加递归文本查找（FindChildButtonByText）兼容不同窗口的节点层级。
8、怪物意图显示：怪物头顶显示当前意图——红色 `[伤害值]`、蓝色 `▲护盾值`、↓ 施加弱化、↑ 自身强化。
9、怪物回合改为异步逐只执行：每只怪物行动之间停顿 0.5s，结束后刷新 UI 再进入下一只；全部执行完毕后自动进入玩家回合。
10、角色受到怪物伤害时，头顶弹出红色 "-伤害值" 数字，在 0.9s 内向上飘移淡出。


## 6.10（第二次修复）

1. **意图显示修复**：UnitInstanceView.tscn 中 IntentionLabel 的 custom_minimum_size.x = 0 导致标签宽度为零、文本不可见，改为 Vector2(180, 120)；size_flags_horizontal 从 4(ShrinkCenter) 改为 3(ExpandFill)
2. **换行符修复**：UnitInstanceView.cs 中 RefreshIntentionDisplay 里意图文本分行的 "\\\\n"（字面反斜杠n）改为 "\\n"（真实换行符）
3. **死亡占位修复**：CardBattleScene.cs 中 CreateEmptyUnitSlot 不再追加 "\\n未配置角色"，UnitInstanceView.cs 中 BindPlaceholder 移除 "等待配置" 文本，死亡占位通过 SetHealthLabelText("已死亡") 在血条位置显示
4. **编译错误修复**：
   - 移除了 UnitInstanceView.cs 中不存在的 ApplyHighlight() 调用
   - 修复 SetAnchorsPreset 参数类型错误
   - 重构了被意外清空的 CardBattleScene.cs，保留所有必要的公开方法和节点引用
   - 将 PlayCardFrom 修正为 PlayHandCard（BattleSytem 的实际方法名）
