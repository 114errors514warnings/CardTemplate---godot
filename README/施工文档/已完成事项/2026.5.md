5.7
1、增加状态配置表，通过该表来配置状态的属性（是否可叠层、回合结束是否衰减等）
2、新增状态牌机制：使用后至于目标的状态牌堆，当状态结束后再置入所属单位的弃牌堆
3、增加结束游戏按钮，需要输入0才能执行

5.8
1、效果系统扩展：新增 AddCost（增加能量）、ClearAllStates、DrawCard、Heal、ShieldSlam（盾牌冲撞）等 Effect；其中 ShieldSlam 伤害 = 攻击力 + 自身护盾值（经状态修改器处理）
2、卡牌新增与接入：新增"深呼吸"（20000003）与"盾牌冲撞"（11002001）两张卡，并完成对应 EffectType、结算分发与 CSV 配置联动
3、状态系统修复：修复不可叠层状态多次打出同名状态牌时仅回收最后一张的问题，改为保留历史绑定回调并在状态归零时统一触发回收
4、抽牌流程修复：修复卡牌效果触发 DrawCard 时抽牌堆不足不洗牌的问题，改为抽牌堆为空时自动将弃牌堆洗回后继续抽牌（与回合抽牌逻辑一致）
5、卡组配置与操作面板增强："新增卡牌"改为增量配置（默认卡组 + 新增卡牌统一洗牌）；新增"删除卡牌"按钮（参数：卡牌ID + 可选数量），战斗开始前可按数量删除，超出则删至0
6、回合结束与关键词体系：玩家回合结束时自动处理手牌弃置；新增 CardKeyWord 系统并实现"保留"关键词，保留牌在回合结束时不会进入弃牌堆（CSV 第8列改为 CardKeyWord，兼容旧 true 写法）

5.9
1、代码重构：创建 BaseButtonCommand 抽象基类，统一管理所有按钮脚本的共享逻辑（LineEdit 查找、BattleSystem 查找、参数解析、控制台输出等）；将 SetPlayerAttack、SetPlayerDefend、SetPlayerHealth、SetPlayerMaxEnergy、AddPlayerEnergyRaw 5 个脚本重构为继承该基类，代码行数从 ~600 行减少至 ~160 行，消除 ~75% 的重复代码

5.13
1、多人战斗：实现至多3名玩家角色同时参战，保留玩家回合/怪物回合两段式流程；同一玩家回合内允许所有存活角色自由穿插行动，并统一处理回合开始抽牌回能、回合结束弃牌、默认卡组初始化、胜负判定、敌我单位收集与战斗信息显示
2、多人操作与配置：BattleSetupData 改为支持多角色与有序槽位；PlayCard 和玩家属性调试按钮支持按 UniqueInGameId 指定玩家；新增“新增角色”“删除角色”按钮，“选择角色”改为按 index 修改槽位，且战前角色配置上限限制为3
3、规则与目标系统：同步更新系统规则文档与使用介绍；怪物多段攻击新增“每击随机目标/整次意图共用同一随机目标”两种模式，并改为使用 MonsterDamageTargetMode 枚举配置，兼容旧参数写法
4、文件读取机制：新增 DataBase/FilePathRegistry.csv 统一管理核心数据路径；LoadingSystem 增加按 key 取路径和加载 CSV 的接口；Init、BattleSytem、StateSystem、AddCharacterCard、AddCharacter、AddMonster、ChooseCharacter 等改为通过注册表 key 读取，且 key 统一整理为 Data.Card.Common、Data.State.Common、Data.Unit.Character、Data.Unit.Monster、Data.Unit.CharacterDefaultDeck

5.18
1、卡牌加载修复：排查并修复 `LoadingSystem` 中卡牌读取存在的双入口问题；原先 `OnInit()` 会递归扫描 `DataBase/Card/`，但 `LoadCardsByKey(Data.Card.Common)` 在缓存为空时仍可能只读取 `通用/通用Card.csv`，导致初始化顺序不同时出现“专属卡牌 CSV 未生效”的问题。现已统一为首次取卡时直接扫描整个 `DataBase/Card/` 目录并合并缓存，消除仅加载通用卡表的隐患
2、状态扩展：在 `通用State.csv` 已配置“虚无”状态的基础上，补充 `StateType.Void = 8` 枚举，并将配置名从 `Emptiness` 统一调整为 `Void`，使状态 ID、代码枚举与配置名称保持一致，便于后续在状态系统和效果系统中继续接入具体机制
3、实现“添加状态”功能，参数格式：目标UniqueInGameID 状态TypeID 状态层数
4、实现“删除状态”功能，参数格式：目标UniqueInGameID 状态TypeID 状态层数（可选）；省略层数时删除该状态全部层数，填写层数时按指定层数删除，并同步刷新局内信息与控制台提示
5、实现“消耗”原子效果：新增 `EffectSystem.ApplyExhaustCards(source, cardUniqueInGameIds)` 与 `BattleSytem.ApplyExhaustCards(...)`，可按传入的卡牌实例 `UniqueInGameId` 数组顺序，将来源玩家手牌/抽牌堆/弃牌堆中的对应卡牌移入消耗牌堆，并在操作面板控制台输出逐张移动结果与缺失卡牌提示
6、实现最小升级系统闭环：新增 `CardKeyWord.InfiniteUpgrade`、`EffectType.UpgradeBattleCard`、`EffectType.UpgradePermanentCard` 与 `CardOperationTargetType`；`Card` 增加永久/局内升级级数和默认卡组来源字段；角色实例新增默认卡组实例并在同一局游戏内跨战斗保留，战斗开始时由默认卡组实例生成战斗实例，永久升级写回默认卡组实例、战斗升级写回战斗实例
7、实现选牌挂起流程首版：`BattleSytem` 新增待选卡牌上下文与“手牌定向选择/手牌随机选择”两类卡牌目标处理；当卡牌效果需要定向选择手牌时，流程会暂停并在“局内”信息中显示提示，同时新增“选择卡牌”按钮，输入手牌顺序后继续结算剩余升级效果
8、补充重剑手升级相关卡牌与显示规则：在 `重剑手Card.csv` 中新增“暂避锋芒”“淬炼”“绝力一击”；扩展卡牌操作目标以支持“随机默认卡组一张”，并为卡牌操作增加“击杀目标后才触发”的条件参数，用于实现“淬炼”的击杀后随机永久升级；局内信息中的升级卡牌名改为 `卡牌名+`，带 `InfiniteUpgrade` 的卡牌改为 `卡牌名+总级数`
9、接入无限升级伤害公式：对带 `InfiniteUpgrade` 的伤害牌，保留 CSV 中的基础额外伤害作为基值，在伤害结算前按 `基础额外伤害 * 1.2^总级数` 计算升级后的额外伤害，并仅对最终结果做一次向下取整；已用于修复“绝力一击”升级后伤害不随级数变化的问题
10、补充文档：在 `README/系统规则/卡牌参数配置说明.md` 中正式记录 `UpgradeBattleCard` / `UpgradePermanentCard` 的参数语义、`CardOperationTargetType` 取值，以及第三参数 `requireKilledTarget` 的含义与“淬炼”配置示例，避免后续继续从代码中倒推配置规则
11、实现本局失血相关功能：新增 `EffectType.DamageByBattleLostHp`，其伤害按“来源攻击力 + CSV额外值 + 本局已失去生命值”结算；`BattleSytem` 增加战斗内失去生命事件次数统计，并在攻击与护盾冲撞造成 HP 伤害时登记事件；局内信息中同步显示每个单位的“本局失去生命值”和“失去生命次数”
12、重构失去生命次数统计入口：将“失去 HP 次数”的登记从 `EffectSystem` 中分散的手动调用，收束到角色/怪物实例的 `HP` setter 统一上报，再由 `BattleSytem.OnUnitHpChanged(...)` 集中处理；同时将次数统计口径收窄为“仅统计玩家角色实例”，减少后续新增掉血来源时的维护成本
13、实现卡牌出牌条件最小闭环：新增 `CardConditionType` 与 `Card.ConditionParams`，CSV 第 9 列可填写条件参数；当前已支持 `NoBattleCardPlayedThisTurn`，在 `BattleSytem.PlayHandCard(...)` 出牌前校验“本回合内该角色尚未打出过战斗牌”，并在成功打出攻击/技能牌后记录本回合战斗牌次数、在玩家回合开始/战斗结束时重置
14、收束卡牌条件模型：取消对 `CardKeyWord.Condition` 的依赖，改为由 `Card.ConditionParams` 作为唯一条件来源；只要 CSV 第 9 列 `ConditionParams` 非空，系统就会在出牌前校验这些条件，避免“关键词和条件参数必须同时填写”带来的重复配置与错配风险
15、将 `Card.ConditionParams` 从裸 `int[]` 收紧为 `CardConditionType[]`：CSV 解析阶段直接产出强类型条件数组，出牌校验不再手动做整数转枚举，后续扩展新条件时类型边界更清晰

5.19
1、补充重剑手运行时卡表首批基础牌：在 `DataBase/Card/重剑手Card.csv` 中新增“攻守兼备”“横扫”“致残打击”“迅捷打击”“闪躲”5 张可由当前系统直接满足的卡牌，分别复用现有 `Shield / Damage / DrawCard / AddState` 组合完成接入，作为与设计表对齐的第一批补卡
2、扩展 `Damage` 参数格式：支持将最后一个参数直接写作命中次数 `hitCount`，默认按 1 次结算；据此将“横扫”从双 `Damage` 配置收束为单条 `Damage, 3;-1;2`，减少多段攻击牌的 CSV 冗余配置
3、优化出牌与多段伤害日志：移除 `PlayCard` 成功回调中重复输出的目标参数说明；对卡牌侧的多段/多目标 `Damage` 结算改为聚合摘要输出，统一显示总命中次数、总伤害，并逐个列出受击单位的护盾与生命值变化，修复“横扫”只显示最后一次命中结果的问题
4、调整重剑手伤害牌文案与攻守兼备效果：为 `重剑手Card.csv` 中所有带 `Damage` 的卡牌描述统一补上“攻击”相关表述；同时为“攻守兼备”补充 `Damage|Shield` 效果，使其从仅防御改为同时具备攻击与防御
5、扩展 `Shield` 参数格式：支持将最后一个参数直接写作防御次数 `shieldCount`，默认按 1 次结算；当前参数个数大于 1 时，前面的参数继续表示额外护盾，因此可以使用 `1;0;2` 表示“防御2次且无额外格挡”；据此将“未雨绸缪”从双 `Shield` 配置收束为单条 `Shield|AddCost`