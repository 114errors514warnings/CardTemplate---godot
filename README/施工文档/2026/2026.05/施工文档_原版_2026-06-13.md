将BattleSystem改成单例模式，并将DebugBattle中BattleSystem的节点删除

添加EndBattle()函数和EndGame()函数，区别在于EndBattle函数仅销毁所有的Monster


待施工事项：
1、当前受到伤害时，角色头顶上没有出现伤害数字（要求为白色，字体有黑色边框），怪物意图的文本没有用颜色区分，现在需要用红色表示攻击，蓝色表示防御，一个向下的箭头表示给玩家角色施加弱化效果，一个向上的箭头表示给自身或其他怪物施加强化效果，多种相同的意图进行数值上的合并，比如攻击3、攻击3、攻击3，合并成为攻击3x3进行显示
2、调试面板中的“结束游戏”按钮需要点击后结束本局游戏并回到战前配置的界面
3、调试面板应当覆盖战斗场景中的所有节点和界面，当前调试面板可能被部分卡牌遮罩
4、当前存在过多敌人时可能会出现网格拉得太长，导致游戏UI无法完整显示在屏幕里的问题，尝试缩小战斗场景中玩家角色和怪物单位的大小

针对完整Demo还需要实现的内容：
1、地图（可选）
2、完整的怪物体系
3、装备系统
4、材料系统（可选）
5、事件系统（可选）
6、美术（必须）
7、多角色卡牌设计（可选，如果不做，只能挑重剑手中不含多人战斗的卡牌）
8、完整的战斗关卡（包含角色模型，卡牌、UI交互，战斗动画）

已完成：

现在将当前的主控台移动到新建的游戏场景中，作为一个可以通过按钮呼出的panel（需要一个关闭按钮，作为一个新建的窗口，不要直接复用DebugBattle场景），仅将“操作面板”内的内容移动到该panel中，并将所有和控制台输出相关的内容移动到Godot的Console中，而不是原来名为Console的组件中（可以考虑新建对应的脚本，避免覆盖原有场景内的功能），将新建的游戏场景设置为启动后呈现的场景，并在战斗开始前弹出一个独立的窗口来进行初始化战斗（复用原有的DebugBattle中新增角色、敌人、新增卡牌以及显示战斗相关情况（显示当前有什么角色、什么怪物、对应角色增加了什么牌）的UI，不要接入DebugBattle，新增一个仅包含DebugBattle上述功能的场景作为窗口）

本次“主控台迁移到新主场景”需求的润色版规格：
1、保留现有 `CardBattleScene` 作为新的游戏主场景，并将项目启动场景切换到该场景。
2、不要复用 `DebugBattle.tscn` 作为窗口；需要新增独立场景来承载调试操作面板和战前初始化窗口。
3、调试操作面板窗口只迁移旧 `DebugBattle` 中 `操作面板` 内的按钮与参数输入能力，通过新主场景中的按钮呼出，并提供关闭按钮。
4、新主场景不再保留旧的 `ConsoleContainer/Console` 可视控制台；在该场景下，所有原本写入场景 Console 的信息都直接输出到 Godot Console。
5、战斗开始前默认弹出独立的初始化窗口；该窗口复用旧主控台里“新增角色、删除角色、选择角色、新增敌人、删除敌人、新增卡牌、开始游戏”这些战前配置能力。
6、初始化窗口需要同时复用旧 `局内信息` 的展示协议，用于显示当前配置了哪些角色、哪些怪物、以及额外加入了哪些卡牌；实现方式应为新窗口内独立节点，不接入 `DebugBattle` 整场景。
7、调试操作面板和初始化窗口都需要作为新建窗口挂载到 `CardBattleScene` 上，与战场主 UI 解耦。

执行规则（新增，后续实现默认遵循）：
- 每次开始实现新的界面或系统需求前，先将原始需求整理为可执行规格，再按该规格实施。
- 润色后的规格需要同步记录在施工文档中，作为后续继续施工时的直接依据。

本次“新角色卡牌战斗场景”需求的润色版规格：
1、创建一个独立的新战斗主场景，沿用当前项目既有战斗规则与数据结构，不改动原有规则定义。
2、场景左侧为最多 3 名角色的网格展示区，右侧为怪物网格展示区；单位卡片下方依次显示名称、黑底红血条、白字生命值文本与状态图标区。
3、状态图标区支持鼠标悬停提示，提示框跟随鼠标移动，并显示状态名称、效果说明与剩余持续时间。
4、中下部为当前角色手牌区；右侧提供角色切换按钮，切换后仅改变当前显示角色，不改变各自手牌顺序。
5、玩家回合中，手牌支持鼠标拖拽打出：
- 需要目标的卡牌：离开手牌区后吸附到屏幕上方横向中线，并显示指向鼠标的箭头；悬停有效目标时箭头变红，松手后对该目标出牌，否则回到原位。
- 不需要目标的卡牌：持续跟随鼠标，若在手牌区外松手则直接出牌，否则回到原位。
6、左下与右下分别提供抽牌堆、弃牌堆按钮，弃牌堆按钮上方提供消耗牌堆按钮；点击后显示全屏半透明黑色牌堆窗口，顶部按玩家显示 Tab，右上角提供关闭按钮。
7、手牌区右上方提供结束回合按钮；仅当当前没有拖拽卡牌时可点击，点击后结束所有玩家回合并进入怪物回合。
8、场景中的卡牌统一使用 `res://Scenes/Card/CardDisplayPrefab.tscn`，并将该预制体的显示文字统一调整为黑色后再投入使用。
9、所有区域必须互不重叠，并保留后续扩展鼠标交互的空间。

我需要你创建一个新的角色使用卡牌进行战斗是场景，需要包含以下内容：
1、左侧，使用网格排列的3个人物（需要可以通过鼠标进行交互，具体交互模式在之后说明），右侧，使用网格排列的若干个怪物（需要可以通过鼠标进行交互，具体交互模式在之后说明），角色和怪物下方都需要依次显示名字、血条（一个长条，使用固定的黑色长条表示完整的血条，使用从左到右的红长条表示当前血量占当前血量上限的百分比，长条中间需要使用白字写明 当前血量/当前血量上限 例如：12/40）、状态图标区（鼠标移动到状态图标区时，显示随鼠标移动的tips，每条tips上显示对应的状态名称、状态效果（在状态效果中顺便说明其持续的剩余时间，例如：易伤 在1回合内受到的伤害增加50%））
2、中下侧，是角色对应的手牌区，手牌区的右侧会有玩家角色数量的按钮，分别对应当前显示的是哪个角色的手牌，点击对应的按钮，手牌区切换为对应角色的手牌（不管怎么切换，手牌的顺序保持不变）；手牌区中的卡牌在玩家回合可以通过鼠标拖动。需要选定目标的卡牌，在拖动离开手牌区域后，固定在屏幕上侧横向中线上，显示一条从卡牌中心指向鼠标的箭头，当鼠标移动到有效的目标身上时，箭头变为红色，反之箭头为白色，如果鼠标移动到有效目标时松开，则对目标使用此卡牌，反之，手牌回到手牌区对应位置，取消打出；不需要选定目标的卡牌，则始终被鼠标拖动，如果鼠标松开的位置在手牌区外，则使用该卡牌，否则手牌回到手牌区对应位置
4、左下角和右下角分别放置一个按钮，代表抽牌堆和弃牌堆，点击对应的按钮，显示一个全屏半透明黑色窗口，按照固定每行卡牌数的格式显示卡牌，窗口上方存在玩家数量的tab（如果只有一个玩家角色则不显示tab），表示对应玩家的抽牌堆/弃牌堆，当点击对应的tab时，抽牌堆/弃牌堆的卡牌显示切换到对应的角色，右上角需要一个关闭窗口的按钮，弃牌堆按钮上方有一个消耗牌堆按钮，这里显示对应玩家的消耗牌堆内容，规则和上述一致
5、手牌区的右上方（不与手牌区重合，在手牌区右部的上侧）存在一个结束回合按钮，当前没有拖动手牌时，点击该按钮会结束所有玩家的回合，来到怪物的回合

战斗的规则和当前的场景规则相同（具体可参见D:\MY\My Game\卡牌模拟器\README\系统规则 中的相关文档），要求上述内容所占部分互不重合，卡牌使用项目中的res://Scenes/Card/CardDisplayPrefab.tscn，并在开始操作前将其中的文本修改为黑色，将我所有的需求用AI润色一边后执行，并将该规则记录下来，每次实现需求前将需求使用AI润色后执行




我先把这几项对应到现有代码结构上，确认状态定义、卡牌关键词、效果分发和战斗统计分别落在哪些类里，再给你一个按改动面拆开的实现思路，避免空泛设计。

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/Entity/Card/Card.cs#1-1), lines 1 to 340

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/DataUnit/State/StateDefinition.cs)

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/System/StateSystem.cs#1-1), lines 1 to 360

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/System/EffectSystem.cs#1-1), lines 1 to 360

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/DataUnit/Card/CardEnum.cs)

我还差两块关键信息：单位实例里现在有哪些可持久字段，以及角色卡组/默认卡组的数据结构怎么存。把这两块补齐后，我能把“永久升级”和“局内统计”分别落到具体层级。

Searched for regex `interface IUnitInstance|class CharacterInstance|class MonsterInstance|StatePile|discardpile|drawpile|handcards|DefaultDeck|CharacterDefaultDeck` (`**/Scripts/**/*.cs`), 170 results

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/Entity/Unit/Character.cs)

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/Entity/Unit/Unit.cs)

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/System/LoadingSystem.cs#1-1), lines 1 to 320

我再补一下 CSV 解析层，尤其是 `State.csv` 和 `Card.csv` 的字段绑定方式。这样可以明确每一项是“只加枚举/字段”还是“需要改 CSV 语义和存档模型”。

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/DataBase/State/%E9%80%9A%E7%94%A8State.csv)

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/System/Modules/Loading/LoadStateCsv.cs)

Read [](file:///c%3A/MY/MY/My%20Game/%E5%8D%A1%E7%89%8C%E6%A8%A1%E6%8B%9F%E5%99%A8/Scripts/System/Modules/Loading/LoadCardCsv.cs)

先给结论：这 9 项里，2、3、5、9、10 属于“在现有架构上可平滑扩展”；4 需要补一套牌堆模型；6、7 可以先做数据层，但 8 一旦要“永久升级角色卡组”，就已经不只是战斗系统改动，而是要引入一层角色卡组进度/存档模型。

我先按你当前代码结构拆一下落点。状态定义和状态规则主要在 StateDefinition.cs、LoadStateCsv.cs、StateSystem.cs。卡牌关键词、效果枚举和卡牌运行时实例主要在 CardEnum.cs 和 Card.cs。战斗过程和牌堆流转主要在 BattleSytem.cs。角色实例和牌堆字段在 Character.cs。默认卡组加载在 LoadingSystem.cs。

**逐项思路**
2. 通用State.csv新增“是否弱化效果 / 是否强化弱化效果”
- 现状里 `StateDefinition` 只有 `IsStackable / IsPermanent / TurnStartDecayAmount`，扩展点是对的。
- 但从建模上，我不建议真的长期保留两个布尔值。因为会出现非法组合：
  - 不是弱化效果，但却是强化弱化效果
  - 是弱化效果，但强化字段为空时语义不清
- 更稳的做法是：
  - CSV 层可以先加两列，兼容你的表格习惯。
  - 运行时在 `StateDefinition` 里落成一个单一语义字段，例如 `StateAffinity` 或 `StateKind`：
    - `None`
    - `DebuffNormal`
    - `DebuffEnhanced`
    - `Buff`
- 如果你坚持按文档保留两列，也建议在解析时做约束：
  - `IsEnhancedDebuff=true` 时自动要求 `IsDebuff=true`
  - `IsDebuff=false` 时 `IsEnhancedDebuff` 强制视为 `false`
- 这部分主要改 StateDefinition.cs、LoadStateCsv.cs、CSV 表头。

3. 效果：解除状态栏的第一个普通弱化状态
- 这个需求的关键不是“删状态”，而是“第一个”怎么定义。
- 你现在 `unit.States` 是 `Dictionary<StateType, StateRuntimeData>`，本身没有稳定“第一个”语义。也就是说，当前架构里“状态栏顺序”和“状态数据存储顺序”是脱钩的。
- 所以根上要先补一个“状态显示顺序”的来源。建议两种方案选一种：
  - 方案 A：单位上维护 `List<StateType> StateDisplayOrder`
  - 方案 B：在 `StateRuntimeData` 增加 `AppliedOrder / FirstAppliedFrame / FirstAppliedSequence`
- 然后“解除第一个普通弱化状态”的逻辑就变成：
  - 按状态栏显示顺序遍历
  - 找到第一个 `DebuffNormal`
  - 调用 `StateSystem.RemoveState`
- 这里建议新增一个独立效果类型，不要复用现有 `ClearState`，因为现有 `ClearState` 是按具体 `StateType` 清除，不是按筛选规则清除。
- 比较合理的是新增：
  - `EffectType.ClearFirstNormalDebuff`
- 主要改 CardEnum.cs、Card.cs、StateSystem.cs。
- 风险点只有一个：如果你不先定义“状态栏顺序”，这个效果会变成不稳定行为。

4. 关键词：消耗
- 这是现在牌堆模型里缺的一层。当前角色只有：
  - `handcards`
  - `drawpile`
  - `discardpile`
  - `StatePile`
- 没有 `ExhaustPile`。
- 所以要从根上补一层：
  - 在 `CharacterInstance` 增加 `exhaustPile`
  - 如果你希望怪物或其他单位未来也支持，`IUnitInstance` 也可以补 `ExhaustPile`
- 出牌后的流转逻辑要改在 BattleSytem.cs：
  - 普通牌：仍进弃牌堆
  - `Retain`：只影响回合结束弃牌
  - `Exhaust`：打出后进消耗牌堆
- 这里有一个必须先定的交互问题：
  - 状态牌如果带“消耗”，是“打出时立即进消耗牌堆”，还是“状态结束后进消耗牌堆”？
- 按你文档“打出后移动到消耗牌堆，不再参与抽牌堆、弃牌堆和角色状态牌堆的循环”，更像是“最终归宿是消耗牌堆”，但对状态牌不能立即移走，否则状态牌堆就挂不住这张牌。
- 所以我建议规则定成：
  - 非状态牌：打出后直接进 `ExhaustPile`
  - 状态牌：状态持续期间仍留在目标 `StatePile`，状态结束回收时，如果有 `Exhaust` 则进拥有者 `ExhaustPile`，否则进 `DiscardPile`
- 这样最符合你现在的 `StatePile -> 状态结束回收` 架构。
- 这项需要改：
  - CardEnum.cs
  - Character.cs
  - BattleSytem.cs
  - 可能还要改战斗信息展示 BattleInfoPresenter.cs

5. 状态：勇气铠甲，每打出一张战斗牌，防御1次
- 这项从架构上很好落。
- 关键是先定义“战斗牌”的范围。按你现在 `CardCategory`，我建议：
  - `Attack` 和 `Skill` 算战斗牌
  - `State` 不算
- 然后在出牌成功后统一挂一个状态钩子，而不是在每张牌逻辑里分散处理。
- 最自然的入口就在 BattleSytem.cs 的 `PlayHandCard` 成功结算后：
  - `card.Apply` 成功
  - 扣费、移牌
  - 触发 `StateSystem.OnCardPlayed(sourcePlayer, card)`
- 再由 `StateSystem` 检查：
  - 如果玩家有 `CourageArmor`
  - 且打出的是战斗牌
  - 则触发一次防御
- 这里的“防御1次”也需要先定语义：
  - 如果是“获得1点护盾”，那就是固定 +1
  - 如果是“触发一次防御效果”，那应等于走一遍 ShieldEffect，即 `source.Defend + 0`
- 我更倾向后者，因为你文案写的是“防御1次”而不是“获得1点护盾”。
- 这项主要改：
  - CardEnum.cs
  - StateSystem.cs
  - BattleSytem.cs

6. 关键词：无限升级，记录级数
- 当前 `Card` 运行时实例没有升级字段，所以这项的第一步不是“做效果”，而是先补卡牌升级数据模型。
- 建议直接做通用升级结构，不要只为“无限升级”单独打补丁：
  - `PermanentUpgradeLevel`
  - `BattleUpgradeLevel`
  - `TotalUpgradeLevel => Permanent + Battle`
- 再加一个关键词：
  - `CardKeyWord.InfiniteUpgrade`
- 这样后面第 7、8 项都能复用。
- 但这里还有一个更深的问题：你现在卡牌的数值来自 CSV 固定字段，系统里没有“升级后效果如何变化”的统一公式。
- 所以这项可以先分两阶段：
  - 第一阶段：先把等级记录下来
  - 第二阶段：再定义哪些卡受升级影响、如何把等级映射到伤害/护盾/描述
- 如果不先拆两阶段，后面会很容易陷入“等级记住了，但数值没地方算”的状态。

7. 效果：战斗内升级卡牌
- 这一项技术上可做，但和现有效果框架有一个结构冲突：
  - 你当前 `Card.Apply` 的 effect target 只支持 `IUnitInstance`
  - 升级卡牌的目标其实是“Card”，不是“Unit”
- 所以这类“操作卡牌本身”的效果，不适合继续硬塞进现有 `EffectSystem.ApplyXXX(source, target)` 这条链。
- 更稳的做法是把效果分两类：
  - 单位效果：继续走现在的 `EffectSystem`
  - 卡牌操作效果：由 `BattleSytem` 或单独的 `CardOperationSystem` 处理
- 比如新增效果类型：
  - `UpgradeBattleCard`
- 然后在 `PlayHandCard` 里检测这个效果类型，并按参数解析“要升级哪张卡”：
  - 本张牌自己
  - 手牌某张牌
  - 抽牌堆某张牌
  - 弃牌堆某张牌
- 如果你现在只想做最小闭环，我建议先只支持一种最简单语义：
  - “升级本张牌自身” 或 “升级当前选中的一张手牌”
- 这能避免先引入一套复杂的卡牌目标选择系统。

8. 效果：永久升级卡牌
- 这是这批需求里改动最大的一项，因为它不是战斗内数据，而是“角色卡组资产”的变更。
- 你当前默认卡组来自 LoadingSystem.cs 加载 CSV，战斗开始时再实例化进抽牌堆。
- 这意味着现在没有“角色拥有的、可被永久修改的卡组实体”。CSV 是模板，不应该在战斗里直接改。
- 所以要做这项，必须引入一层新数据：
  - 例如 `CharacterDeckProgress`
  - 里面记录角色当前持有卡牌、每张卡的永久升级等级
- 战斗开始时再根据这层数据实例化：
  - 模板卡牌
  - + 永久升级等级
  - + 本局战斗升级等级初始为 0
- 也就是说，第 8 项不是简单新增一个效果就完事，而是需要补“角色卡组进度模型”。
- 这项我建议单独立一个小里程碑，不要和别的效果混着做。
- 如果你现在还没有存档系统，最小实现也至少要先有一份内存中的 `DeckProgress` 结构，不然“永久升级”没有落点。

9. 效果：攻击，额外造成本局战斗中失去生命值的伤害
- 这项其实比较顺，因为你在 BattleSytem.cs 里已经有 `GetBattleLostHp(IUnitInstance unit)`。
- 也就是说，“本局失去生命值点数”你已经有基础能力了。
- 这里主要要定清楚“取谁的失去生命值”：
  - 攻击者自己本局失去的生命值
  - 还是目标本局失去的生命值
- 文案直觉更像“自己越残血越强”，也就是取 `source`
- 实现上最简单的方式是新增一个效果类型：
  - `DamageByBattleLostHp`
- 或者把它作为 `Damage` 的一种伤害修正参数。
- 我更建议新增独立效果类型，原因是：
  - CSV 更直观
  - 日志更清楚
  - 不会把 `Damage` 的参数语义搞乱
- 然后内部就是：
  - `bonus = BattleSytem.Current.GetBattleLostHp(source)`
  - 再走一次攻击结算

10. 战斗局内记录每个玩家对应的失去生命次数
- 这一项建议不要散落在 `HP` 属性 setter 里硬算，否则以后直接改 HP 的地方一多，语义会失控。
- 你现在已经有：
  - “失去生命点数快照”通过 `BattleInitialHpSnapshots` 计算
- 但“失去生命次数”是事件型统计，需要单独存。
- 推荐在 BattleSytem.cs 里新增：
  - `Dictionary<int, int> BattleHpLossEventCounts`
- 然后在真正造成 HP 下降的统一入口里调用：
  - 比如 `EffectSystem.AttackEffect` 中，当 `hpDamage > 0` 时
  - `BattleSytem.Current.RecordHpLossEvent(target, hpDamage)`
- 但从根上讲，未来如果还有“自残”“毒”“燃烧”“直接扣血”之类效果，这种统计最好不要只埋在 `AttackEffect`。
- 更稳的长期方案是引入统一的 HP 变更入口，例如：
  - `BattleSytem.ApplyHpLoss(target, amount, source, reason)`
- 所有掉血都走它，这样第 9 和第 10 项就都不会漏记。

**建议的实现顺序**
1. 先做 2 和 3  
原因：状态元数据和“普通弱化状态”的概念要先稳定。
2. 再做 10 和 9  
原因：第 9 项可以复用已有“失去生命值点数”统计，第 10 项补齐事件型统计后，后面伤害类效果会更稳定。
3. 再做 5  
原因：它只是加一个“出牌后状态钩子”，成本不高。
4. 再做 4  
原因：要补 `ExhaustPile`，会碰到牌堆流转和状态牌回收。
5. 再做 6 和 7  
原因：先把升级字段和“局内升级”打通。
6. 最后单独做 8  
原因：它需要角色卡组进度模型，和战斗系统不是一个层级。

**这批需求里最需要先定规则的点**
1. “第一个普通弱化状态”的“第一个”到底按什么顺序  
是添加顺序、显示顺序，还是固定优先级顺序。
2. “消耗”与状态牌的关系  
是状态结束后进消耗牌堆，还是压根不允许状态牌拥有消耗关键词。
3. “勇气铠甲”的“防御1次”  
是固定获得 1 点护盾，还是走一次完整防御结算。
4. “战斗内升级卡牌”升级的是哪张牌  
自己、手牌选中牌、牌堆某张牌，还是随机。
5. “永久升级卡牌”是否要跨战斗、跨重启保存  
如果要，就已经需要存档/进度层。
6. 第 9 项额外伤害取攻击者还是取目标的“本局失去生命值”

如果你要，我下一步可以直接把这 9 项整理成一份“实施拆分文档”，按：
1. 数据层改动
2. 战斗层改动
3. CSV 改动
4. 高风险点
这样的结构写回 施工文档.md。
