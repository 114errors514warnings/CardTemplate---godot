将BattleSystem改成单例模式，并将UI_Main中BattleSystem的节点删除

添加EndBattle()函数和EndGame()函数，区别在于EndBattle函数仅销毁所有的Monster


待施工事项：
stateSystem









按照你所说的实现方法设计，状态运行时的数据仅包括前3项，是否可叠加/叠加规则将配置在State.csv中

在实现之前，请先在通用State.csv中配置“易伤”状态，包括其对应的StateEnum（在代码中配置）

我希望将State的实现拆分成如下几个部分：
1、最原始的State，原子状态（拿“易伤”举例，对应增加受到的伤害，增伤百分比由传入的参数决定，使用StateEnum（从0开始，0为None，后面为原子状态的开始））
2、在通用State.csv中配置状态包含了哪些原子状态（比如“易伤”只包含“增加受到的伤害”这个原子状态，配置这个的StateEnum表示包含这个状态，并仿照之前Card结构对参数的处理配置所需的参数params，以及该）

叠层        衰减        回合相关机制
可叠层      每回合衰减百分比    持续回合数可叠加
不可叠层    保持不变            持续回合数不可叠加
            每回合衰减特定数量  持续回合无限（可能有其他的解除方式，比如特定行动后解除或者每回合衰减）
            
2、状态的叠层、衰减、回合相关机制
    1.叠层机制包括：
        ·不可叠层
        ·可叠层，叠层后不影响同种状态的持续回合
        ·可叠层，叠层后刷新同种状态的持续回合并将同种状态的数量合并
    2、衰减机制包括：
        ·每回合衰减百分比（需要参数）
        ·每回合衰减特定数量（需要参数）
        ·保持不变
    3、回合相关机制包括：
        ·施加同种状态时，持续回合数不叠加
        ·施加同种状态时，