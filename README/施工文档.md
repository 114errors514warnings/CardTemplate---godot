将BattleSystem改成单例模式，并将UI_Main中BattleSystem的节点删除

添加EndBattle()函数和EndGame()函数，区别在于EndBattle函数仅销毁所有的Monster


待施工事项：
暂未验证创建Instance后显示对应UniqueID的功能是否正常
抽牌堆和弃牌堆卡牌显示（考虑做成可显示顺序或乱序的样式）
Effect需要额外参数的解决方案（effect可能需要一个参数或者多个参数）
stateSystem